services:
  app:
    build:
        context: .
    image: car-simulator-app:latest
    container_name: car-simulator-app
    working_dir: /var/www
    volumes:
        - .:/var/www
        - ./storage:/var/www/storage
    ports:
      - '8000:8000'
